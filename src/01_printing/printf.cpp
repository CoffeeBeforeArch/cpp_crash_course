// Prints "Hello, World!" to the console using printf
// By: Nick from CoffeeBeforeArch

// Include from the C library for I/O
#include <cstdio>

// All C++ program require a main functions (entry point to the program)
int main() {
  // Print "Hello, World!" to stdout
  printf("Hello, World!\n");

  // Returning 0 means we did not encounter an error
  return 0;
}
